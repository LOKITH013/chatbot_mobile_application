import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  chatArea: {
    flex: 1,
    paddingBottom: 12,
  },
  footerText: {
    color: '#ACACBE',
    textAlign: 'center',
    padding: 6,
    fontSize: 12,
  },
});
